/**
 * Monee publishes its own app screenshots, so the preview comes from those.
 * The seeded capture this replaced is in the file's history: the root is a
 * marketing page now, and the app sits behind a login at /app that no amount
 * of seeded localStorage gets past. The other published screen is
 * /app-budgets.png; the dashboard carries more of the product.
 */
export const image = '/app-home.png';
